# Verification Log

Dated records of what was checked, how, and what was seen. Status claims in the [README](README.md) and the ICD should point here. Evidence files are in [evidence/](evidence/). Entries whose raw data was not kept say so.

| Date (UTC) | Item | Method | Result | Evidence |
|---|---|---|---|---|
| 2026-09-25 | CT headless end-to-end (replay) | `adsb-playback` replay of `10_adsb_20220413_060850.csv.gz` → `adsb-console --headless` → `tools/cue_capture.py`, unicast on the RF Collection Desktop | 733 of 733 messages valid, 0 sequence gaps, `track_cue` with up to 3 opportunities, 5 snapshots completed. SIGTERM gave exit 0 plus a `stopping` heartbeat; loss of the source gave exit 1 | Not kept |
| 2026-09-25 | Textual worker defect in CT | Headless run with a 5 s snapshot interval | The first snapshot cancelled the SBS reader (`exclusive=True` shares a worker group). Fixed and regression-tested in ADSB-remoter `12e91fc` | ADSB-remoter test `test_cue_snapshot_worker_does_not_cancel_source_monitor` |
| 2026-09-25 | Opportunity-bearing cues dropped as oversize | Serializer size check; capture statistics | 11 opportunities ≈ 25 kB (no history), over the 16 384 B limit, so dropped. Before the fix, the capture showed 0 opportunities per cue | Led to CR-1 (ADSB-remoter `65e2fbe`) |
| 2026-09-25 | Pi clock | `date` over SSH against the NTP-synced desktop; `chronyc tracking` | Pi was 14.9 s slow; chrony was unsynchronised (PPS and NMEA unreachable, no internet) | Not kept (see CR-8) |
| 2026-09-25 21:07 | Pi clock after NAT to the internet | `chronyc tracking`; repeated SSH offset | chrony synced to internet NTP (`Leap status: Normal`, 0.3 ms); desktop − Pi within SSH jitter (+0.05 to +0.09 s) | Not kept |
| 2026-09-25 | CT CPU on the Pi | `build_track_prediction` over 60 replay tracks, 2 observers × 16 UHF emitters | 407 ms per prediction on the Pi 4, against 84 ms on the desktop | Not kept |
| 2026-09-26 00:50 | Live multicast, Pi → desktop | `adsb-cue` running headless on the Pi (local dump1090) → `cue_capture.py` joined on `eno1` | 34 messages, 0 schema failures, 0 gaps; `track_cue` with 3 opportunities; 2 snapshots consistent; clean `stopping` heartbeat | [cue_traffic_20260926T0050Z.jsonl](evidence/cue_traffic_20260926T0050Z.jsonl) (first 16 messages) |
| 2026-09-25 21:31 | Pi reboot recovery | Pi rebooted | The route, DNS, NTP sync, ZeroTier (`ONLINE`) and dump1090 all came back. `adsb-cue` started before dump1090, exited 1, and was restarted by systemd after 10 s; cues then resumed | Not kept |
| 2026-09-26 02:10 | MATLAB consumer (CueListener) live | `runCueListener` in R2026a joined `239.192.10.1` on `eno1` | 7 messages decoded (heartbeats and a snapshot), 0 gaps, 0 decode errors; followed the sender status from `degraded` to `running` | flightTest `feature/adsb-cue-listener`, CueListener tests (13 passing) |
| 2026-09-26 12:34 | **WI-7 acceptance** (CT 1.1.0) | socat capture from the multicast group (Leif), validated offline against the ADSB-remoter `schemas/*-1.1.0.json` | 46 messages; 0 schema failures, 0 gaps, 0 duplicate ids; 4 snapshots with `published_track_count` equal to cues received (3 carrying `track_cue`). **Meets the Verified criteria** for heartbeat, snapshot begin/end and `track_cue`. No withdrawal observed | [cue_traffic_20260926T1234Z.jsonl](evidence/cue_traffic_20260926T1234Z.jsonl); CR-3 |
| 2026-09-26 14:30 | **Compiled MATLAB app** (Architecture open item 4, in part) | [deployability/cueProbe.m](analysis/deployability/) built with MATLAB Compiler and run on the Runtime (`run_cueProbe.sh`) | `isdeployed` = 1, Java 1.8. Joined `239.192.10.1` on `eno1` and decoded live heartbeats, a snapshot and a `track_cue` with `jsondecode`. Inflated a Python-compressed cue (deflate with dictionary) byte-exact. Runtime needs: base, standard and graphics add-ons only | [analysis/deployability/README.md](analysis/deployability/README.md); CR-2, CR-5 |
| 2026-09-26 | CT 2.0.0 size estimate | [analyze_cue_traffic.py](analysis/analyze_cue_traffic.py) functions applied with the design's clean-up; dictionary trained on the other capture | Median `track_cue` 536 B compressed (4 528 B as plain JSON); about 63 B per extra opportunity. Single aircraft, so to be confirmed | [CT_Message_2.0_Design.md](analysis/CT_Message_2.0_Design.md) |
| 2026-09-26 | Compressed-cue decode in MATLAB | [check_matlab_decompress.m](analysis/check_matlab_decompress.m); Python-produced payloads | gzip and raw deflate with a preset dictionary both decode byte-exact through `java.util.zip`, at 0.1–0.4 ms per cue (`jsondecode` 0.3 ms) | analysis/ |
